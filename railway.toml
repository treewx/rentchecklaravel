[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c '${START_COMMAND:-sleep 5 && php artisan config:cache && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT}'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10